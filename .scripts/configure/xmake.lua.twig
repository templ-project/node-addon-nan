set_languages("c99", "cxx11"){# set_languages('cxx14') #}

add_ldflags('-undefined dynamic_lookup')

target('main')
set_filename('main.node'){# build path to be build\Release #}

set_kind('binary')

-- add node.js headers
-- add_includedirs(os.getenv('NODE_HEADERS'))
{% for folder in folders %}
add_includedirs('{{folder}}')
{% endfor %}

add_files('$(projectdir)/src/*.cc')
